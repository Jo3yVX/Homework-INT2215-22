6.2
Chương trình chạy vĩnh viễn.
Lý do: hàm H() sẽ được gọi mãi mãi không dừng do không có base case.

6.3
Hiện tượng:  Chương trình chạy vĩnh viễn.
Lý do: hàm H() được gọi với tham số không đổi n, dẫn đến việc gọi hàm không dừng lại.

6.4
Với n = 5000 (hoặc lớn hơn), hàm H() sẽ được gọi 5000 lần (hoặc nhiều hơn), gây ra stack overflow do bộ nhớ stack là có hạn.

6.5  //code trong file .cpp
6.6  //code trong file .cpp


